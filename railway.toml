# https://docs.railway.com/config-as-code
# Сборка через Dockerfile: Node (client) + Python (server). Кастомный Railpack buildCommand
# не подтягивал Python в образ.
[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
